# Operator logos

Drop operator logo files here using the exact filenames listed in
`data/operators-overrides.json`. PNG, JPG, or SVG all fine — keep them roughly
square, ≥ 200×200 px, transparent background where possible.

Expected filenames:

- `optibet.png`
- `klondaika.png`
- `laimz.png`
- `olybet.png`
- `tonybet.png`
- `lvbet.png`
- `verde.png`
